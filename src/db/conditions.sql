/*
  interface Condition {
    id: number
    // specifies type of check
    type: "equals" | "gte" | "lte"
    // which field to check
    field: string;
    // what value to expect
    // what field to compare?
    expected: any;
  }
*/
