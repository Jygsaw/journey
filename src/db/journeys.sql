/*
  interface Journey {
    id: number;
    // name to display
    name: string;
    // ruleId foreign key for access control
    ruleId: number;
    // array of placeIds for current journey
    planned: number[];
    // array of placeIds for places actually visited
    visited: number[];
*/
